% aastex6.1 will be released as part of TeXLive 2017 around June 1. 
\documentclass{aastex6}

% Make the underscore behave correctly.
\usepackage[T1]{fontenc}
% Import images
\usepackage{graphicx}
% Used for consistent handling of Figures/Sections/Tables
\usepackage{cleveref}
% Format URLs correctly
\usepackage{url}
\usepackage{amsmath}
\usepackage[T1]{fontenc}
\usepackage{aecompl}
\usepackage{color}

% Want to have a separate storage folder for my figures.
\graphicspath{{./fig/}}

% Settings for cleveref to make Figures and Tables more appropriately
% defined.
\crefname{figure}{Fig.}{Figs.}
\Crefname{figure}{Fig.}{Figs.}
\crefname{table}{Table}{Tables}
\Crefname{table}{Table}{Tables}
\crefname{section}{Section}{Sections}
\Crefname{section}{Section}{Sections}

\newcommand{\vsini}{\ensuremath{v \sin i}}
\newcommand{\Kepler}{\mbox{\textit{Kepler}}}
\newcommand{\Gaia}{\mbox{\textit{Gaia}}}
\newcommand{\McQuillan}{\citep{McQuillan14}}
\newcommand{\Teff}{\ensuremath{T_{eff}}}
\newcommand{\logg}{\ensuremath{\log(g)}}
\newcommand{\kms}{\textrm{km~s}\ensuremath{^{-1}}}

\newcommand{\gvs}{\authorcomment1}

\shorttitle{Rotation Contamination in \Kepler{}}
\shortauthors{Simonian et al.}
\begin{document}

\title{Evaluating Rotation Contamination in \Kepler{} using APOGEE}
\author{Gregory V. Simonian}
\affil{Department of Astronomy, The Ohio State University}
\affil{140 West 18th Avenue, Columbus, OH 43210}
\email{simonian@astronomy.ohio-state.edu}

\begin{abstract}
To be determined.
\end{abstract}
\keywords{stars: rotation}

\section{Introduction}

One of the significant results from the \Kepler{} satellite has been the
determination of rotational periods for a large sample of old, field stars
\citep{Basri11,Affer12,Nielsen13,Reinhold13,McQuillan14,Garcia14}.
These rotation rates are expected to be extremely useful to deriving a field
gyrochronology, which can lead to a better characterization of the ages of
\Kepler{} stars, and hence a better understanding of the discovered planets.

The \Kepler{} field was designed to point outside of the plane of the galactic
disk, making the field biased toward old stars. Despite the old age of the
field, there is still a population of rapid rotators at the level of 1\% of the
population which have rotation rates significantly higher than expected from
existing gyrochronological relations. One possible explanation for these rapid
rotators are that they are tidally-synchronized binaries. The synchronized
binaries are useful because they provide insight into the formation of binaries
at scales too small for disk fragmentation to occur. 

While there have been many attempts to evaluate the determination of rotation
periods in stars throughout the \Kepler{} field, they have all been through
comparisons between different periodogram analysis, but not with independent 
measures of rotation, such as \vsini.

One of the valuable tools in performing this analysis is a uniform derivation
of the radii of these targets. The initial attempt at characterizing stars and
providing radii was the original KIC \citep{Brown11}, which fit broad-band
\textit{griz} and an intermediate-band \textit{D51} photometry to
\citet{Castelli04} stellar atmospheres and \citet{Girardi00} isochrones.
High-resolution spectroscopy found that radii from the original KIC were found 
to be systematically underestimated \citep{Everett13}. Additionally, around 5\%
of \Kepler{} targets had KIC parameters which were considered unphysical for an 
old field population \citep{Batalha13}. 

\citet{Huber14}, and the updated methodology in \citep{Mathur17} attempt to
address the shortcomings in the original stellar parameter classification by
re-adjusting the KIC parameters to conform to more current DSEP isochrones 
\citep{Dotter08}, and also used more precise input stellar parameters such as
asteroseismology and spectroscopy. In addition to refitting the original KIC 
stellar parameters, \citet{Huber14} derives stellar parameters for the 7\% of 
observed \Kepler{}  
targets without KIC stellar parameters due to missing photometric measurements. 
\citet{Huber14} classified these stars using 
asteroseismology, 2MASS colors, and \Kepler-INT Sloan photometry
\citep{Greiss12}. Because
the APOGEE-2 targeting program \citep{Zasowski17} was based on \citep{Huber14}
stellar parameters without regard to their presence in the original KIC, and
serendipitously observed many of these missing targets, providing a check on
how successful the \citet{Huber14} methodology worked in the absence of full
photometry, 

In this paper, we look at the rotation distribution of \Kepler{} targets as
seen from APOGEE \vsini{}s. APOGEE's limited resolution will only make it
sensitive to the most rapid rotators, particularly on the cool end. However,
this is exactly the population we're interested in. 

\section{Sample Selection}
\label{sec:sample}

In order to perform a comparison between rotation period and \vsini{}, we must
also have a sample where the stellar radius can be
characterized. 

\subsection{APOGEE}

The base of our sample comes from the Data Release 14 \citep{Abolfathi18}
of the APOGEE survey \citep{Majewski17}. The workhorse behind the survey is the
APOGEE spectrograph, a high-resolution (\(R \sim 22,000\)) multi-fiber
near-infrared spectrograph \citep{Wilson10} mounted on the SDSS 2.5-meter
telescope at Apache Point Observatory \citep{Gunn06}.

Reduction of the APOGEE data takes place in three main stages. First,
individual visit spectra are reduced, including detector calibration, bad pixel
masking, wavelength calibration, sky subtraction, and determination of
individual radial velocities through cross-correlation of template spectra.
Second, the individual spectra are combined by correcting for radial velocity
differences between the exposures, either by cross-correlating with each other,
or with template spectra, whichever leads to a smaller scatter
\citep{Holtzman18}. The full process is detailed in \citet{Nidever15}.

The final step is the extraction of stellar parameters and chemical abundances
by the APOGEE Stellar Parameter and Chemical Abundances Pipeline (ASPCAP)
\citep{GarciaPerez16}. ASPCAP measures stellar parameters by performing a
chi-squared minimization \citep{AllendePrieto06} over a 6-dimensional space of
\Teff, \logg, [M/H], [\(\alpha\)/M], \vsini, and microturbulent velocity. Once
those six parameters have been determined, ASPCAP measures abundances for
individual elements, including \feh{}.

After determination of the stellar parameters and chemical abundances, the 
effective temperatures are calibrated to the photometric system of 
\citet{GonzalezHernandez09} using a metallicity-dependent offset, and the 
abundances are calibrated to provide homogeneous results within clusters. 
After calibration, the scatter between the 
calibrated and photometric temperatures  were found to be a function of 
\Teff{}, ranging from 130 K at 5500 K down to 85 K at 4000 K
\citep{Holtzman18}. The statistical uncertainty in \feh{} was measured to be 
0.009 dex \citep{Holtzman18}, but the systematic uncertainty is likely closer
to 0.1 dex \citep{Serenelli17}.

We inspected the fits to a set of spectra representative of targets flagged 
by the ASPCAP pipeline as having potential quality problems. A small fraction
(2.5\%) of targets have the \STARBAD{} quality flag enabled. Visual inspection
of a representative sample of spectra indicated that the flag was largely the
result of poor subtraction or normalization of the spectrum in the pipeline, or
due to poor model fits on the cool (\(\Teff < 4250 K\)) end. We exclude these
from our analysis. A substantially larger fraction of targets (14\%) have the 
\STARWARN{} quality flag enabled. Visual inspection of a representative sample 
of these spectra indicated that the fits reasonably resembled the underlying 
spectra. We include all targets with the \STARWARN{} quality flag enabled. 

An additional 807 targets have the \VSINIWARN{} quality flag enabled. However, 
all but 28 of them have \vsini{} < 3 \kms, meaning they are certainly below the
detection limit of APOGEE\@. \gc{Look through a bunch of these to make sure
    that somehow there aren't broad lined spectra that are fit with a narrow
model}. Viewing a representative sample of these targets shows that most of
these targets do have narrow-lined spectra corresponding to low
\vsini{}. Because these objects are so below the detection threshold, any systematics in
the numerical values of \vsini{} will not affect our results. The remaining 28
targets 

\begin{figure*}[htb]
    \centering
    \epsscale{1.1}
    \plotone{f2}
    \caption{\emph{Left:} \Teff-\MK{} diagram for the APOGEE observations of
        \Kepler{} targets. Asteroseismic targets are shown as black dots. The
        dwarf sample is shown as brown dots. The light blue dots indicate 
        eclipsing binary targets and purple dots are \Kepler{} Objects of 
        Interest. A binary sequence is clearly visible on the lower main
        sequence. Temperatures are spectroscopic APOGEE temperatures. A 
        representative error bar for the cool dwarf sample is 
        shown in the bottom-left corner, although the vertical error bar is too
        small to be easily visible. \emph{Right:} A density plot of the 
        full APOGEE sample. To preserve the dynamic range of the dwarf 
        sequence, the red clump was allowed to saturate. The bin size is 100 K
    in temperature and 0.02 mag in \(Ks\)-band absolute 
magnitude.}\label{fig:apogee_selection}
\end{figure*}

\Cref{fig:apogee_selection} shows all \Kepler{} targets with observed
spectroscopic temperatures in the APOGEE DR14, color-coded by observing
program. One of the driving efforts for \Kepler{} field observations has been
the spectroscopic characterization of asteroseismic targets, shown as small
black dots \citep{Zasowski17,Pinsonneault18}; 11,734 targets are part of that
sample. The asteroseismic sample is substantially biased toward giants 
and subgiants due to their high amplitude oscillations. A complementary 
program proposed to observe the remaining portion of the \Kepler{} sample 
cooler than 6500 K and brighter than \(H < 11\), make up an additional 3193
targets, shown as small red dots. An additional 695 targets were part of an 
effort to characterize \Kepler{} planet hosts with \(H < 14\), shown as purple 
diamonds. The remaining 102 targets were targeted to follow-up 
eclipsing binaries \citep{Prsa11,Slawson11}, and are shown as light blue 
circles.  

The APOKASC asteroseismic dwarf and subgiant sample
consists of 426 targets \gvs{Check this number} which presented
solar-type oscillations in \Kepler{} short-cadence data
\citep{Chaplin11}, and were observed in APOGEE DR13
\citep{Majewski17}. Using asteroseismic scaling relations, the radii
for these objects can be determined to an accuracy of around 3\%
\citep{Serenelli17}. It should be noted that the asteroseismic sample
is highly biased compared to the field population. Subgiants are
overrepresented because the
amplitude of solar-type oscillations increases as stars evolve. 
Additionally, the nondetection
of oscillations in rapidly-rotating giants that ought to be detectable
indicates that the asteroseismic sample may be biased against rapid
rotators \citep{Gaulme14,Tayar15}. 

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.8\textwidth]{APOGEE_flags_APOGEE}
  \caption{APOGEE Properties of the two samples studied in
  this paper. The asteroseismic and cool dwarf samples are indicated by
red and black dots, respectively. \gvs{Remove blue dots from this figure. And
demarcate the STAR\_WARN groups somehow}. }
  \label{fig:targeting}
\end{figure}

\gvs{Update these numbers!}
We selected 426 asteroseismic dwarfs from the APOKASC catalog v4.2.4. To 
obtain a high-quality sample for further study, we removed three objects with 
the STAR\_BAD flag because they tended to be cases with bad subtraction or
normalization in the ASPCAP pipeline. We include 51 objects with 
the STAR\_WARN and 46 objects with the VSINI\_WARN flags after further 
inspection because we observed that their ASPCAP stellar parameters are
generally reliable. All targets with \(\vsini \ge 10\) \kms were visually
inspected for double-lined spectra. Four were excluded due to the obvious
presence of double-lines, and are included in \cref{tab:dlsb}. 91 of
the remaining stars have period detections in \citet{McQuillan14}, 240 were
period nondetections, and 88 did not fall under the selection criteria in 
\citet{McQuillan14}. The full asteroseismic dwarf sample with subsets of those
having McQuillan period detections, non-detections, and being unanalyzed are 
shown in the upper left panel of \cref{fig:targeting}

Objects targeted under the APOGEE-2 APOKASC dwarf program were selected to have
\(5000 \textrm{ K} \le \Teff \le 6500 \textrm{ K}\) and \(\logg > 3.5\) as
determined by \citet{Huber14} \citep{Zasowski13}. Since the targeting criteria
were based on the \citet{Huber14} analysis, both targets with original KIC
values as well as targets with stellar parameters determined exclusively by 2MASS 
colors. In total, there are 2640 targets with the
\texttt{APOGEE2\_APOKASC\_DWARF} flag enabled. 1980 had stellar parameters in
the original KIC while 660 were not, a substantial 25\% increase in the number
of stars with stellar parameters. The subsets with and without KIC parameters
are shown in the top right and bottom left panels of \cref{fig:targeting},
respectively. Of the targets with KIC parameters, 64 had
the \texttt{STAR\_BAD} flag set, while 28 of those targets without KIC
parameters had the \texttt{STAR\_BAD} flag set; Since these objects only
account for 3\% and 4\% of the samples with and without KIC targets,
respectively, they are left out of the comparative analysis.

We define the cool star regime as the region in
the HR-diagram of \citet{Huber14} parameters, where the subgiant branch for
stars older than the field turnoff are well-separated from the
main-sequence. The separation is prominent for \(T < 5450\) K with a
dividing line at \(\log g = 4.3\), as illustrated in 
\cref{fig:targeting}. In this regime, uncertainties
in the radius must be due to uncertainties in the \logg{}, not
ambiguities in the mass. Additionally, stellar evolution occurs slowly enough
in this mass range that the change in radius over typical ages of these stars 
is expected to be small.

We restrict ourselves to a known, well-defined sample by selecting
objects with the \texttt{APOGEE\_KEPLER\_COOLDWARF} flag. They were
originally targeted as a magnitude-limited same with having original KIC 
\citep{Brown11} \(\Teff < 5500\) K and 
\(\logg > 4.0\)\footnote{\url{http://www.sdss.org/dr12/algorithms/ancillary/apogee/kepler_dwarfs/}}. 
We include a few targets on the original list which were observed later
in APOGEE2 under the targeting flag \texttt{APOGEE2\_APOKASC\_DWARF}. We also 
perform the same quality checks as with the
asteroseismic sample: remove STAR\_BAD targets and check for
double-lined spectra. The cool dwarfs are shown in red in
\cref{fig:targeting}.


Since APOGEE observed a large sample of targets withou original KIC parameters,
it is capable of validating the methodology of the \citet{Huber14} parameters.

\subsection{\Gaia{}}

The availability of high-quality parallaxes from the \Gaia{} mission's Data
Release 2 \citep{Gaia18} enables both the determination of
radii for the APOGEE sample, as well as evolutionary state classifications for
stars. The \Gaia{} DR2 performed a fully consistent single-star
5-parameter (\(\alpha\), \(\delta\), \(\mu_\alpha\), \(\mu_\delta\), \(\pi\))
solution to 1.3 billion sources over 22 months of observations
\citep{Lindegren18}. For targets which could not be adequately fit with a
single-star 5-parameter solution, a 2-parameter (\(\alpha\), \(\delta\))
solution is performed instead \citep{Michalik15}. The criteria for a
2-parameter fall-back solution include targets with Gaia \(G > 21.0\) mag, 
having fewer than 6 visibility periods of observations, and an error ellipse 
larger than a magnitude-dependent limit in its largest dimension 
\citep{Lindegren18}.

We use the cross-matched database of \citet{Berger18b} to match \Kepler{} targets
against \Gaia{} DR2 detections.
\citet{Berger18b} cross-matched targets in the KSPC DR25 \citep{Mathur17}, 
with \Gaia{} DR2 sources using both position and \(G\)-band flux. Of the 
successfully cross-matched targets, \citet{Berger18b} excluded objects with 
fractional parallax errors greater than 0.2, \(\Teff < 3000\) K, 
\(\logg < 0.1\), and low quality 2MASS photometry.  Because the remaining
targets have high-quality parallaxes, we use the traditional 
formula for deriving distance modulus from parallax instead of the Bayesian 
method advocated by \citet{Luri18} to reduce computational complexity. We
include a zero-point offset of 0.05 mas \citep{Zinn18}.

\subsection{Photometric Data}

In order to characterize our stellar sample, we choose to use
\(Ks\)-band photometry to minimize the impact of extinction. \(Ks\)-band 
apparent magnitudes were measured by the 2MASS survey \citep{Skrutskie06}, 
which uniformly performed profile-fit photometry and derived 
uncertainties for the full \Kepler{} field.  The typical \(Ks\)-band 
photometric uncertainty for the APOGEE sample is 0.017 mag. A very small 
fraction of targets (0.3\%) of the 
APOGEE sample have 2MASS photometry flagged due to blending. In the case of
blending, aperture photometry is performed instead, and there is no
well-characterized uncertainty in the photometry. In order to ensure
that all the photometry in our sample is uniformly analyzed, we excluded these
blended objects. \gvs{Do I want to exclude blended objects?}

The 
typical extinction from the KSPC and its uncertainty for cool dwarfs is 
\(A_V = 0.33\), and \(\sigma_{A_V}=0.025\). Applying the \citet{Cardelli89} 
relation that \(A_K/A_V = 0.114\), the effect of extinction itself shrinks to 
the level of photometric errors.  


\subsection{Comparison Datasets}

\subsubsection{\vsini{}}

In order to understand the characteristics of \vsini{} measurements in APOGEE,
we compare the APOGEE \vsini{} to other overlapping large spectroscopic
surveys. By comparing the APOGEE \vsini{} to \vsini{} measured by other
instruments with known characteristics, we can estimate both the uncertainty in
the APOGEE \vsini{} as well as the detection threshold for APOGEE.

The spectroscopic survey of \citet{Bruntt12} measured \vsini{} for 93
solar-type asteroseismic targets in the \Kepler{} field. These 
observations were taken using two R=80,000 spectrographs on the
3.6-meter Canada--France--Hawaii Telescope and 2-meter Bernard Lyot
Telescope. While uncertainties are not quoted in \citet{Bruntt12},
previous analyses have determined \vsini{} uncertainties of about 0.6
\kms \citep{Bruntt10a,Bruntt10b}.

In contrast to the hotter subgiants in \citet{Bruntt12}, we also compare APOGEE
\vsini{} to cool dwarfs in the Pleiades. \citet{Stauffer87} measured
\vsini{} for 100 low-mass stars in the Pleiades. Spectra for these stars were
observed with a resolution of R\(\sim 60,000\) on the 1.5-meter telescope and
the 4.5-meter Multiple Mirror Telescope at Mt. Hopkins. While Pleiades targets 
in general do not have the rotational
distribution seen in the field, their young age enables a broad
distribution of \vsini{}s to be checked against for a wide range
of spectral types \gvs{Actually check the \Teff{} range in the overlap
sample}. 

\section{\vsini{} Comparison}
\label{sec:vsini}

The analysis performed in this manuscript relies heavily on
measurements of rotation period, stellar radius, and \vsini. Because we
adopt rotation periods from \citet{McQuillan14}, we refer the
reader to that work, as well as to \citet{Aigrain15} for a detailed 
discussion of the quality of their and other period-finding algorithms.

As mentioned in Section~\ref{sec:sample}, the asteroseismic stars are a
benchmark on how precisely stellar radii can be measured. For a detailed
discussion of the uncertainties of using grid-based asteroseismic
scaling relations for the APOKASC dwarfs, we refer the reader to
\citet{Serenelli17}. 

For the cool dwarf sample, we use radii reported in the Kepler Stellar 
Properties
Catalog\footnote{\url{https://exoplanetarchive.ipac.caltech.edu/docs/Kepler_stellar_docs.html}}
\citep[a.k.a.\ revised KIC]{Mathur17}, derived from DSEP isochrones 
\citep{Dotter08} based on the methodology of \citet{Huber14}. 
However, objects with \(5500 \textrm{ K } > \Teff > 5000 \textrm{ K }\) are 
specially denoted as having uncertain radii due to confusion with subgiants. 
\gvs{We need to show that APOGEE spectroscopy significantly reduces that 
uncertainty}. The DR25 version of the Kepler Stellar Properties Catalog only
incorporates APOGEE spectroscopic parameters for targets in
SDSS DR12. In order to bring the radii of more recently observed objects onto
the same calibration, we use DSEP isochrones to calculate a radius
offset between the stellar parameters given in the revised KIC and
between the APOGEE stellar parameters to adjust the revised KIC radius. 
\gvs{Since the Revised KIC uses DSEP, would this be
equivalent to just pluggin in the APOGEE parameters into DSEP\@? We also
need to quantify the uncertainty in radiusfor these objects.
\citet{Huber14} reports something like 10-15\%.}

\vsini measurements was were only incorporated into the ASPCAP pipeline in 
SDSS DR13, and have not been vetted and calibrated to the same extent as the 
other 
stellar parameters. We check the validity of the APOGEE \vsini{}s in
this work by comparing 
them to previous samples with measured \vsini{}s in the two regimes we are
interested in.


\begin{figure}[htb]
  \centering
  \includegraphics[width=0.8\textwidth]{bruntt_apogee_vsini_comparison}
  \caption{Comparison between APOGEE \vsini{} and the \vsini{} from
  higher-resolution spectra analyzed in \citet{Bruntt12} for
  \Kepler{} asteroseismic targets. At high \vsini{}, the scatter in
  \vsini{} appear consistent with 10\%. There is an abrupt change in
  scatter at 7 \kms, which we interpret as being the \vsini{} detection
  limit for APOGEE. }
  \label{fig:bruntt_vsini}
\end{figure}


A comparison between the APOGEE and \citet{Bruntt12} targets is shown in
\cref{fig:bruntt_vsini}. This figure indicates that the scatter is
discontinuous at \citep{Bruntt12} \(\vsini=7\kms\). We interpret this
discontinuity as setting the detection limit of APOGEE, 
where stellar rotation is actually detected in the high-\vsini{} regime.
The scatter in the fractional error is consistent with 10\% for the
detections, so we adopt that as the uncertainty in the APOGEE \vsini{} values.

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.8\textwidth]{APOGEE_Stauffer_Hartmann_Comparison_Good}
  \caption{Comparison between the overlap between APOGEE observations
    and those made in \citet{Stauffer87}. The red dashed lines indicate
  the detection limits for the two measurements. Objects which are
nondetections in both surveys are omitted. Above the detection limits,
the two \vsini{} measurements are in good agreement.}
  \label{fig:APOGEE_Stauffer_Hartmann_Comparison_Good}
\end{figure}


\section{Rotation Comparison}

\subsection{Rapid Rotator Criteria}

Since much of our analysis focuses around classifying targets into rapid and
slow rotators, we require a definition of rapid rotator which is meaningful
from both spectroscopic and photometric points of view. 

Due to the temperature-dependent nature of the stellar radius, any flat cut in
period or \vsini{} space will necessarily be temperature dependent in the
transformed space. We characterized this dependence by fitting a second-order
polynomial to \citet{Huber14} radii for \citet{Huber14} dwarfs with \(4250
\textrm{ K } \le \Teff \le 5450\). Determination of a star as a \citet{Huber14}
dwarf is done by a flat cut at \(\logg > 4.2\). The hottest part of our sample
was excluded from the fit due to the strong dependence of the radius on age 
evolution; the coolest part of the sample was excluded due to poor model fits
in APOGEE spectra. The observed spread in radii is driven by inferred metallicity
differences in the models. \gc{Add figure on this. And maybe it would be worth
doing the lower envelope instead of the median.}

In addition to fitting to the \citet{Huber14} radii, we also determine a
\Teff-radius relation on the dwarf sequence using DSEP isochrones directly.
This approach also allows us to quantify the uncertainty imparted by the
age-dependence on radius.

As discussed in \cref{sec:vsini}, we established a reliable detection limit for
APOGEE \vsini{}s to be 10 \kms. This cutoff translates to a temperature (and
metallicity)-dependent cutoff in period space. Likewise, a flat period
dependence ends up becoming a temperature-dependent cutoff in period space.
\gc{Add double-panelled figure that demonstrates this.} In order to have full
completeness, we select a flat period cutoff of 3 days so that all
\vsini{} measurements will be above the detection limit. We report rapid
rotator fractions using both flat \vsini{} and flat period cut criteria.

\subsection{Asteroseismic Dwarfs}

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.8\textwidth]{Asteroseismic_radii_vsini_check}
  \caption{\emph{Left:} ASPCAP \vsini{} compared to the inferred
equatorial velocity for APOKASC dwarfs with vsini detections. 5/27
objects have \vsini{} values larger than the inferred equatorial
velocities. \emph{Right:} Asteroseismic radii compared to radii inferred from
\vsini{} and the rotation period. The points above the line are the same
as the points above the line on the left.\label{fig:astero_radii}}
\end{figure}


\gvs{Double-check numbers here} In the APOKASC sample with 
\citet{McQuillan14} periods, 27 have 
\vsini{} detections (\(\vsini > 7\) km/s) using DR14 values. The 
correspondence between the expected and observed velocities for the objects 
with \vsini{} detections are shown in \cref{fig:astero_radii}. As expected, 
most of the stars lie below the equality line, corresponding to having 
\(\sin i \le 
1\).  There are five objects which have unphysical solutions, which are shown
in \cref{table:bad_astero}.

\input{tables/bad_astero.tex}

Of these five objects, three (KIC 8677016, 8952800, 10518551) have
\vsini{} values close to the detection threshold. Because the errors of 
marginal \vsini{} detections are complex, 
it may be possible that these are outliers. KIC 3223000 has a
\vsini{} which is slightly above the predicted equatorial velocity. It
is a 1.7-\(\sigma\) difference based on the \vsini{} uncertainty, which
is consistent with being a statistical fluctuation. Additionally, it can
be explained by 4.9 day measured rotation period actually being a 
half-period detection alias, implying that the real equatorial velocity to be twice 
as large. 

The final outlier, KIC 9908400, is so significantly off of the relation
that it can not be explained by either uncertainties or aliasing. However,
it may be adequately explained by contamination of the \Kepler{} PSF\@.
KIC 9908400 has a \Teff{} which places it above the Kraft break. If the 
photometric variation originated in a cooler star within 
the Kepler PSF, the mismatch between the period and \vsini{} would
be explained.

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.8\textwidth]{Velocity_vsini_APOKASC_apogeeteffs}
  \caption{Comparison of inferred equatorial velocities and
    \vsini{} across effective temperature for asteroseismic dwarfs. Blue
  points are predicted equatorial velocities while red diamonds are
  measured \vsini{} values from DR14. If the equatorial velocity is
  greater than \vsini{}, then the two points are joined by a black line.
If the points imply \(\sin i > 1\), then they are joined by a red line.
The detection threshold of 7 km/s is shown as the red dashed line.
For 28 objects, both the predicted equatorial velocities and the measured
\vsini{}s are beneath the detection threshold, hence they are omitted from 
this plot.\label{fig:velmatch_teff}}
\end{figure}

To see if this mismatch is prevalent among the hot stars because they
have much weaker magnetic fields (van Saders, in prep), we checked 
if the velocity mismatch trends with effective temperature.
\Cref{fig:velmatch_teff} compares the predicted equatorial velocities to
\vsini{} for objects with varying effective temperatures. There's no
apparent trend where hotter stars tend to be more mismatched. However,
this does not preclude KIC 9908400 from being a contaminant if this sort
of contamination is rare.

\begin{figure}
  \centering
  \includegraphics[width=0.8\textwidth]{apokasc_vsini_comparison}
  \caption{\emph{Top:} Observed distribution of \vsini{} (red) compared
  to the theoretical distribution of \vsini{} (black). The theoretical
distribution was obtained by taking the inferred equatorial velocity
distribution (blue) and convolving it with \(\sin i\) and a 10\%
uncertainty. \vsini{} non-detections are treated separately; out of 75 
objects, 43 non-detections were predicted and 42 were observed.
\emph{Bottom:} Cumulative distribution of \vsini{} including the impact
of non-detections. Small statistical blips are seen around 15 and 20
km/s, but according to a KS test, these two distributions are consistent
with being identical (\(p=0.5\))\label{fig:apokasc_vdists}}
\end{figure}

The overall distribution of \vsini{} is consistent with the inferred
velocity distribution. We tested this by taking the velocity
distribution inferred from the periods, convolving it with a Gaussian of 
width 10\%, and then convolving that distribution with a distribution of
randomly-oriented \(\sin i\) \gvs{I should cutoff the \(\sin i\)
distribution at \(1/2\) instead of 0, since \(\sin i < 1/2\) won't be
seen if there are spots.} The results are shown in
\cref{fig:apokasc_vdists}. As expected, the number of targets in each
\vsini{} bin tracks the theoretical prediction, including non-detections. To 
quantify this agreement, a KS-test indicated that the \vsini{}s were 
consistent with being drawn from the theoretical distribution at the
\(p=0.5\) confidence level. We note that the  convolutions were performed 
on the binned dataset as opposed to the full distribution function, which 
introduced 
integration errors on the order of 2\%. However, decreasing the bin sizes 
didn't affect the conclusion.

\subsection{Cool Dwarfs}

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.8\textwidth]{cool_stars_velocity_radius}
  \caption{\emph{Left:} ASPCAP \vsini{} compared to the inferred
equatorial velocity for cool APOKASC dwarfs with vsini detections. 5/27
objects have \vsini{} values larger than the inferred equatorial
velocities. \emph{Right:} Asteroseismic radii compared to radii inferred from
\vsini{} and the rotation period. The points above the line are the same
as the points above the line on the left.\label{fig:cool_stars_velocity_radius}}
\end{figure}

In this sample, the \vsini{}, period, and radius indicates heavy 
disagreement, as seen in
\cref{fig:cool_stars_velocity_radius}. For a large number of objects
the \vsini{} is too large given the observed period and radius. The
disagreement is significantly more severe for the dwarfs than the
subgiants. Therefore, we explore what could go wrong in radius, \vsini{}, or
rotation period.

\subsubsection{Radii determination}

One of the obvious places where things can go wrong is with the stellar
radii. Subgiants misclassified as dwarfs would have equatorial
velocities that are severely underestimated by the \citet{Huber14}
pipeline. While misclassified subgiants wouldn't be able to explain the
objects with very large discrepancies in
\cref{fig:cool_stars_velocity_radius}, it could plausibly be behind the 
objects with \(R\sin i \sim 2 R_\sun\).

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.8\textwidth]{cool_apogee_logg_comparison}
  \caption{Dwarfs and subgiants classified by \citet{Huber14} placed in
  an HR diagram with APOGEE spectroscopic parameters. Most of the 
  \citet{Huber14} dwarfs (blue crosses) are on the APOGEE dwarf locus.
  The \citet{Huber14} subgiants (red diamonds) are found both in the
  subgiant branch as well as the dwarf locus. \vsini{} detections are
  denoted as squares, with a magenta fill indicating a \citet{Huber14}
  subgiant and a white fill indicating a \citet{Huber14} 
  dwarf.\label{fig:cool_apogee_logg_comparison}}
\end{figure}

In order to determine whether this misclassification could be an issue,
we compare the dwarf/subgiant classification from \citet{Huber14} to
that which would be obtained from APOGEE spectroscopic \logg{}
measurements. While the spectroscopic \logg{} measurements for dwarfs
are uncalibrated, and should not be taken at face value, we assume that
they should be able to distinguish between subgiants and dwarfs, at the
very least. The APOGEE spectroscopic measurements are shown in
\cref{fig:cool_apogee_logg_comparison}. Due to the uncalibrated nature
of the APOGEE \logg{}s, the main sequence does not behave as expected,
particularly at the cool end. However, a clear main-sequence and
subgiant branch can be distinguished. The APOGEE measurements indicate
the surprising result that it is not the subgiants that are
misclassified as dwarfs by \citet{Huber14}; rather APOGEE indicates that
dwarfs are being misclassified as subgiants. This exacerbates the
problem of the radii being underestimated.

For objects confined to the main sequence, radius uncertainties can come from
loosely constrained ages, uncertainties in measured effective temperature, and
uncertainties in the metallicity. We model the radius uncertainty over the main
sequence by taking DSEP isochrones at solar metallicity between 1 and 14 Gyr,
and calculating the radius change over that time. That evolution in radius is
shown in \cref{fig:dsep_radius}. 

Uncertainties due to metallicity are expected to be low, as APOGEE spectra are
able to constrain metallicity relatively to within 0.05 dex \citep{Holtzman15}.
According to the DSEP isochrones, this metallicity uncertainty translates to a
radius uncertainty of \gc{Fill this in}.

There is also an uncertainty due to the assignment of effective temperatures. 
APOGEE claims an accuracy of effective temperatures on the order of 100 K. 

% Are Asteroseismic sample in DR13?
% Compare to hot star sample NOT in DR13.

\section{Results}

\subsection{Huber colors}

Based on \cref{fig:targeting}, we don't find that there is a significant bias
in the type of objects that the original KIC failed to determine stellar
parameters for, and those that it successfully determined stellar parameters
for. \gc{Based on a method of resampling from the distribution}

% From the sample that overlaps with APOGEE, we find that the sample of
% rapidly-rotating objects with high \vsini{} follows a distribution
% consistent with the rotation periods found by Rafa. One of the ways you
% can see this is by noting that the distribution of periods follows the
% distribution predicted by \vsini{} assuming stellar radii as predicted
% by the DSEP models.

% We also find that the period distribution derived from photometric
% variability seems to follow that predicted by the \Kepler{} eclipsing
% binaries.

% The correspondence between \vsini{} and rotation period indicates that
% we are finding the rapidly rotating populations in the \Kepler{} field
% out to a contamination level of \emph{something}

\section{Discussion}

\section{Conclusion}


\bibliographystyle{aasjournal}
\bibliography{references}

\end{document}




